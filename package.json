{
  "name": "voxel-sandbox-engine",
  "version": "0.2.5",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1",
    "typecheck": "tsc --noEmit",
    "test": "tsc --noEmit && node scripts/run_tests.mjs"
  },
  "dependencies": {
    "@vitejs/plugin-basic-ssl": "^2.1.0",
    "three": "^0.175.0",
    "vite": "^6.3.0"
  },
  "devDependencies": {
    "@types/three": "^0.184.0",
    "typescript": "^5.8.3"
  }
}
